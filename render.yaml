services:
  - type: web
    name: glassbox-scheduler
    env: python
    plan: free
    rootDir: sih/block_planner
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir backend app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
